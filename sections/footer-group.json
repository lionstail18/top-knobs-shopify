/*
 * Footer group — points at the custom Top Knobs footer (sections/tk-footer.liquid).
 * The Shopify admin theme editor may rewrite this file. If the editor adds back
 * the default footer blocks (text / link_list / newsletter), reset by changing
 * "type" back to "tk-footer" and clearing the blocks.
 */
{
  "type": "footer",
  "name": "Footer Group",
  "sections": {
    "footer": {
      "type": "tk-footer",
      "settings": {}
    }
  },
  "order": [
    "footer"
  ]
}
